{% render 'partials/head.liquid' %}

{% render 'partials/menu.liquid' %}

<h1>Nieuws</h1>
<form action="/zoeken" method="GET">
    <input type="search" id="searchbar" name="search" placeholder="Zoek in nieuws..." autocomplete="off">
</form>

<details>
    <summary><span>Sorteer artikelen</span><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-chevron-down-icon lucide-chevron-down"><path d="m6 9 6 6 6-6"/></svg></summary>
    <ul>
        <li class="sort-list"><a href="/algemeen-nieuws-nieuw-oud">Nieuw-oud</a></li>
        <li class="sort-list"><a href="/algemeen-nieuws-oud-nieuw">Oud-nieuw</a></li>
    </ul>
</details>

<article class="grid-container">
{% for story in stories %}
    <section class="test-news">
        {% if story.cover.id %}
            <img src="https://fdnd-agency.directus.app/assets/{{ story.cover.id }}" alt="">
        {% else %}
            <img src="/afbnotfound.svg" alt="Geen afbeelding beschikbaar">
        {% endif %}

        <h2>{{ story.title }}</h2>
        <p>{{ story.intro }}</p>

        <time>
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-calendar-icon lucide-calendar"><path d="M8 2v4"/><path d="M16 2v4"/><rect width="18" height="18" x="3" y="4" rx="2"/><path d="M3 10h18"/></svg>
            {% if story.date %}
                <span>{{ story.date }}</span>
            {% else %}
                <span>Datum onbekend</span>
            {% endif %}
        </time>
    </section>
{% endfor %}
</article>

{% render 'partials/foot.liquid' %}